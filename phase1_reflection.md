# Phase 1 Reflection

The Phase 1 code loads a decay CSV file, validates the Time_s and Counts columns, sorts the readings by time, creates a raw decay plot, and exports a summary table. The half-life is estimated with the first-half method by finding the first time where the count value falls to half of the initial count or lower.
